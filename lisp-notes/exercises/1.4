;If b is greater than zero,
;then it is added to a
;If it is less than or equal to zero,
;it is subtracted from a.
;Since a double subtraction equals an addition,
;in either case the absolute value is added to a
;
;The if statement returns a primitive operator + or -
;and is positioned such that the operator
;will serve as the procedure for the arguments a and b